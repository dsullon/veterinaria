<div class="row">
  <div class="col-sm-12" id="featured">
    <div class="page-header text-muted">
      Detalle de Requerimientos
    </div>
  </div>
</div>


<div class="row"> 
  <div class="col-sm-12">

<table>
  <thead>
    <tr>
      <th>Requerimiento</th>
      <th>Servicio</th>
      <th>Mascota</th>
      <th>Comentarios</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @request_details.each do |request_detail| %>
      <tr>
        <td><%= request_detail.request %></td>
        <td><%= request_detail.service %></td>
        <td><%= request_detail.pet %></td>
        <td><%= request_detail.comment %></td>
        <td><%= link_to 'Show', request_detail %></td>
        <td><%= link_to 'Edit', edit_request_detail_path(request_detail) %></td>
        <td><%= link_to 'Destroy', request_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Request detail', new_request_detail_path %>
</br>
</br>
<%= link_to 'Regresar', requests_path %>

  </div>
</div>
