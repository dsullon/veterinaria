<div class="row">
  <div class="col-sm-12" id="featured">
    <div class="page-header text-muted">
      Raza de mascotas
    </div>
  </div>
</div>

<div class="row"> 
  <div class="col-sm-12">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Nombre</th>
          
          <th colspan="3"></th>
        </tr>
       </thead>

      <tbody>
        <% @breeds.each do |breed| %>
          <tr>
            <td><%= breed.name %></td>
            
            <td><%= link_to 'Editar', edit_breed_path(breed) %> | 
              <%= link_to 'Eliminar', breed, method: :delete, data: { confirm: 'Está seguro de eliminar?' } %>
            </td>
          </tr>
       <% end %>
      </tbody>
    </table>
    <br>
    <%= link_to 'Nueva Raza', new_breed_path %>
  </div>
</div>