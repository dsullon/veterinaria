<% @vets = Vet.all %>
<table class="table">  
    <% @vets.in_groups_of(3, false) do |vet| %>  
    <tr>  
      <% vet.each do |v| %>  
      <td>
      	<address>
    		<strong><%= v.name %></strong><br>
		  	<%= v.address %><br>
		  	<abbr title="Teléfono">Tel: </abbr><%= v.phone %><br>
		  	<a href="http://<%= v.url %>"><%= v.url %></a>
		</address>      	
      </td>  
      <% end %>  
    </tr>  
    <% end %>  
</table>